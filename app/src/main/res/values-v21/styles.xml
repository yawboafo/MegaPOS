<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <item name="android:colorPrimary">@color/limeOne</item>
        <item name="android:colorPrimaryDark">@color/limeprime</item>
        <item name="android:colorAccent">@color/accentColor</item>
    </style>

</resources>
